spring:
  datasource:
    url: 'jdbc:postgresql://127.0.0.1:5432/postgres'
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: update